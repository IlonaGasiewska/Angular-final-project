import { GameHistorySortPipe } from './game-history-sort.pipe';

describe('GameHistorySortPipe', () => {
  it('create an instance', () => {
    const pipe = new GameHistorySortPipe();
    expect(pipe).toBeTruthy();
  });
});
